/**
 * These are the <em>Plain Old Java Objects(POJOs)</em> that hold domain
 * information for this application. These models are only used in
 * internal operation of application. When operating with the database use the
 * models in {@link era.uploader.data.database.jooq} and when operating with
 * GUIs please use ViewModels {@link era.uploader.data.viewmodel}.
 * {@link era.uploader.data.converters} can be used to convert between database
 * models and domain models.
 */
package era.uploader.data.model;